<div class="container">
<h2>Add a reminder</h2>

<%= simple_form_for ([@user_subscription, @reminder]) do |f| %>
  <%= f.input :date, as: :date, html5: true %>
  <%= f.input :details %>
  <%= f.button :submit %>
<% end %>
</div>
